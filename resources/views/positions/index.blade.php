@extends('layouts.admin') @section('title','Cargos') @section('page-title','Cargos') @section('page-subtitle','Cargos organizados por área')
@section('content')<x-ui.table-card title="Listado de cargos" data-refresh-container><x-slot:actions><a class="btn btn-primary btn-sm" href="{{ route('positions.create') }}" data-modal-url="{{ route('positions.create') }}" data-modal-title="Nuevo cargo">Nuevo cargo</a></x-slot:actions><table class="table table-hover"><thead><tr><th>Cargo</th><th>Área</th><th>Tipo</th><th>Personal</th><th>Estado</th><th class="text-end">Acciones</th></tr></thead><tbody>@forelse($positions as $position)<tr><td>{{ $position->name }}</td><td>{{ $position->area->name }}</td><td>{{ $position->is_academic ? 'Académico' : 'Administrativo' }}</td><td>{{ $position->personnel_count }}</td><td>{{ $position->is_active ? 'Activo' : 'Inactivo' }}</td><td class="text-end"><div class="table-actions-stack"><a class="btn btn-outline-primary btn-sm" href="{{ route('positions.edit',$position) }}" data-modal-url="{{ route('positions.edit',$position) }}" data-modal-title="Editar cargo">Editar</a><form method="POST" action="{{ route('positions.destroy',$position) }}" data-confirm-delete="¿Eliminar el cargo {{ $position->name }}?">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm">Eliminar</button></form></div></td></tr>@empty<x-ui.empty-row colspan="6" message="No hay cargos registrados."/>@endforelse</tbody></table><x-slot:footer>{{ $positions->links() }}</x-slot:footer></x-ui.table-card>@endsection
